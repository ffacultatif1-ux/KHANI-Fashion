# KHANI Fashion - Configuration Apache

# Encodage par défaut
AddDefaultCharset UTF-8

# Sécurité : bloquer l'accès aux dossiers api/config et api/includes
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

# Sécurité : bloquer l'accès aux fichiers sensibles (secrets, config)
<FilesMatch "^\.env|\.env\.|config\.php$">
    Require all denied
</FilesMatch>

# Sécurité : scripts de maintenance / migration inaccessibles depuis le web
<FilesMatch "^(install|migrate_to_supabase)\.php$">
    Require all denied
</FilesMatch>


# Page d'accueil par défaut
DirectoryIndex index.html

# Compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json
</IfModule>

# Cache des assets
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>
